apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-toto-MonApppp-frontend-ingress
  namespace: staging-toto-MonApppp-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.testdomain.flapi.org
      secretName: staging-toto-MonApppp-frontend-tls
  rules:
    - host: staging.testdomain.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-toto-MonApppp-frontend-service
                port:
                  number: 3000
