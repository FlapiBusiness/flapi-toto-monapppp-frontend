apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-toto-MonApppp-frontend-deployment
  namespace: staging-toto-MonApppp-frontend
  labels:
    app: staging-toto-MonApppp-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: staging-toto-MonApppp-frontend
  template:
    metadata:
      labels:
        app: staging-toto-MonApppp-frontend
    spec:
      containers:
        - name: staging-toto-MonApppp-frontend
          image: corentin35/toto-MonApppp-frontend:staging-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '400m'
              memory: '512Mi'
            limits:
              cpu: '800m'
              memory: '1Gi'
      imagePullSecrets:
        - name: regcred
