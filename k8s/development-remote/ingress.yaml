apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-toto-MonApppp-frontend-ingress
  namespace: dev-toto-MonApppp-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - dev.testdomain.flapi.org
      secretName: dev-toto-MonApppp-frontend-tls
  rules:
    - host: dev.testdomain.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: dev-toto-MonApppp-frontend-service
                port:
                  number: 3000
