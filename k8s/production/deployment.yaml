apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-toto-MonApppp-frontend-deployment
  namespace: prod-toto-MonApppp-frontend
  labels:
    app: prod-toto-MonApppp-frontend
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-toto-MonApppp-frontend
  template:
    metadata:
      labels:
        app: prod-toto-MonApppp-frontend
    spec:
      containers:
        - name: prod-toto-MonApppp-frontend
          image: corentin35/toto-MonApppp-frontend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: '1'
              memory: '1Gi'
            limits:
              cpu: '2'
              memory: '2Gi'
      imagePullSecrets:
        - name: regcred
